//Searching a no. from an array
#include<stdio.h>
int main()
{
    int  i,n,m;
    printf("Enter the size of an array ");
    scanf("%d",&n);
    int a[n];
    printf("Enter the elements of an array ");
    for(i=0;i<n;i++)
        scanf("%d",&a[i]);
    printf("Enter the no. to be searched ");
    scanf("%d",&m);

}