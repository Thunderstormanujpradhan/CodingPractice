// Write a program to reverse the even index position and odd index position for even element array and odd element array
#include<stdio.h>
int main()
{
    int size;
    printf("Enter the the size of an array : ");
    scanf("%d",&size);
    int arr[size];
    
}